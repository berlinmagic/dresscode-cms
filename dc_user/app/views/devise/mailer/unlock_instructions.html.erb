<p><%= t('dc.users.login.hello_user') %> <%= @resource.email %>!</p>

<p><%= t('dc.users.mailer.account_has_been_locked') %></p>

<p><%= t('dc.users.mailer.click_link_to_unlock') %></p>
<br/>
<p><%= link_to t('dc.users.mailer.unlock_my_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
<br/>
