<% content_for :sidebar do %>

<div class="side_box">
	<div class="sidebar_head <%= 'aktiv_box' if @header && @u_aktiv != "new" %>">
		<%= link_to t('dc.prefs'), dc_settings_path %>
	</div>
	<ul class='sidebar'>
		<li><%= link_to 'Account', dc_settings_path+ '/user', :class => "#{'aktiv' if @u_aktiv == 'user'}" %></li>
		<li><%= link_to 'C-M-S', dc_settings_path('cms'), :class => "#{'aktiv' if @u_aktiv == 'cms'}" %></li>
		<li><%= link_to 'Mail-Server', dc_settings_path('mail'), :class => "#{'aktiv' if @u_aktiv == 'mail'}" %></li>
	</ul>
</div>



<div class="side_box">
	<div class="sidebar_head <%= 'aktiv_box' if @header && @u_aktiv != "new" %>">
		<a href="#"><%= t('strange_preferences.fine_modul_settings') %></a>
	</div>
	<ul class='sidebar'>
		<%= hook :admin_settings_sidebar %>
	</ul>
</div>

<div class="side_box">
	<div class="sidebar_head <%= 'aktiv_box' if @header && @u_aktiv != "new" %>">
		<a href="#"><%= t('strange_preferences.fine_style_settings') %></a>
	</div>
	<ul class='sidebar'>
		<% if current_user && site_master && ( 12 == 14 ) %>
			<li><%#= link_to 'Style-Modul', admin_fine_styles_path, :class => ('aktiv' if @aktivio == 'stylez') %></li>
		<% end %>
		<li><%= link_to 'Optik', dc_settings_path('optik'), :class => "#{'aktiv' if @u_aktiv == 'optik'}" %></li>
		<li><%= link_to 'Stylez', dc_settings_path('stylez'), :class => "#{'aktiv' if @u_aktiv == 'stylez'}" %></li>
	</ul>
</div>

<%#= help_link_box %>




<% end %>